version: '3.8'

services:
  postgres:
    image: postgres:14.6-alpine
    environment:
      - POSTGRES_USER=username
      - POSTGRES_PASSWORD=password
      - POSTGRES_DB=questionsdb
    ports:
      - "5432:5432"
    networks:
      - app-network

networks:
  app-network:
